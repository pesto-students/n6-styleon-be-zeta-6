//Model will be imported( firestore )

const getbookingData = async (res,req) => {
    /* 
        query to fetch data from firestore
        return the data to the service layer

    */
    console.log("calledd booking repo")
    const response = true;
    return response
}


module.exports = {
    getbookingData
}